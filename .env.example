# Bridge WebSocket server
BRIDGE_WS_PORT=8080

# OSC output target (e.g. Unreal Engine, Blender, TouchDesigner)
OSC_TARGET_HOST=127.0.0.1
OSC_TARGET_PORT=39539

# Client dev server
VITE_WS_URL=ws://localhost:8080
