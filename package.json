{
  "name": "onapose",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "shared",
    "client",
    "bridge"
  ],
  "scripts": {
    "dev": "concurrently -n client,bridge -c cyan,yellow \"npm run dev -w client\" \"npm run start:dev -w bridge\"",
    "build": "npm run build -w shared && npm run build -w client && npm run build -w bridge",
    "lint": "npm run lint -w client && npm run lint -w bridge"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
